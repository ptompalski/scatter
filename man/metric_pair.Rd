% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_display.R
\name{metric_pair}
\alias{metric_pair}
\alias{metric_format}
\title{Metric Display Helpers}
\usage{
metric_pair(value, percent, format = "{value} ({percent}\%)")

metric_format(metric, format = "{value}")
}
\arguments{
\item{value}{A yardstick-style metric function for the primary value.}

\item{percent}{A yardstick-style metric function for the relative or percent value.}

\item{format}{A format string using `{value}` for single metrics, or `{value}`
and `{percent}` for paired metrics. Numeric format specifiers such as
`{value:.1f}` and `{percent:.0f}` are supported.}

\item{metric}{A yardstick-style metric function.}
}
\value{
A metric display specification for use in the `metrics` argument.
}
\description{
Helpers for controlling how metrics are displayed in labels created by
`agreement_metrics()` and `scatter()`. The underlying metrics are still
calculated by `yardstick`; these helpers only affect label formatting.
}
\examples{
metrics <- list(
  "RMSE" = metric_pair(yardstick::rmse, rrmse, "{value:.1f} ({percent:.0f}\%)"),
  "bias" = metric_pair(md, rmd, "{value:.1f} ({percent:.1f}\%)"),
  "R2" = metric_format(yardstick::rsq, "{value:.2f}")
)

}
